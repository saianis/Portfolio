'use strict';

describe('travelApp.home module', function() {

  beforeEach(module('travelApp.home'));

  describe('Home controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var homeCtrl = $controller('HomeCtrl');
      expect(homeCtrl).toBeDefined();
    }));

  });
});